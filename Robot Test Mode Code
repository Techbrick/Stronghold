void Robot::Test() {
	int ID = 0;
	while (IsEnabled()) {
		CANTalon *talon = new CANTalon(ID);
		if (driveStick.GetRawButton(3)) {
			ID++;
			if (ID > 13) {
				ID = 0;
			}
		}
		float testMove = -driveStick.GetRawAxis(1);
		talon->Set(testMove);

		SmartDashboard::PutNumber("talon input value", talon->GetAnalogInRaw());
	}
}
